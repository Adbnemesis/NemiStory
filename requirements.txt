# NemiStory Python Dependencies
# Local offline TTS (MLX on Apple Silicon) & Audio Processing Pipeline

# Core audio manipulation & signal processing
numpy>=1.26.0
scipy>=1.12.0
soundfile>=0.12.1
sounddevice>=0.4.6

# Apple Silicon local TTS framework (optional on non-macOS platforms)
mlx>=0.20.0; sys_platform == 'darwin'
mlx-metal>=0.20.0; sys_platform == 'darwin'
mlx-audio>=0.5.0; sys_platform == 'darwin'

# Hugging Face & tokenizer models
transformers>=4.40.0
tokenizers>=0.19.0
safetensors>=0.4.0
huggingface_hub>=0.23.0

# CLI & terminal formatting utilities
click>=8.1.0
rich>=13.7.0
tqdm>=4.66.0
pyyaml>=6.0.1
